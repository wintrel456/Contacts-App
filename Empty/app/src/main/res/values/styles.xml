<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/bottom_panel</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:navigationBarColor">@color/bottom_panel</item>
        <item name="android:statusBarColor">@color/bottom_panel</item>
        <item name="colorControlActivated">@color/side_color</item>
        <item name="colorSwitchThumbNormal">@color/side_color</item>
        <item name="android:colorForeground">@color/second_side_color</item>
    </style>

    <style name="splash_screen" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>

    <style name="stroke_add_button" parent="AppTheme">
        <item name="android:strokeColor">@color/Font</item>
    </style>

    <style name="progress_bar" parent="Widget.AppCompat.ProgressBar">
        <item name="android:indeterminateTint">@color/Font</item>
    </style>

</resources>